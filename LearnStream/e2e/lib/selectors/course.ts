// Course-list / catalog / course-detail-page selectors live here. Empty as
// of Wave 0 — the original lib/selectors.ts only ever held login/signup
// helpers (now in ./auth.ts), nothing course-page-related has needed a
// shared selector yet. See lib/selectors.ts (the barrel) for the note on
// where new selector helpers belong going forward.
export {};
