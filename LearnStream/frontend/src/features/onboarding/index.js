// Public API surface of the onboarding feature (docs/lanes/auth.json owns
// features/onboarding/**). Created empty in Wave 0 so the directory and its
// barrel file exist for AUTH to fill in — other features must only ever
// import from a feature's index.js, never reach into its internals.
export { OnboardingPhoneStep } from './OnboardingPhoneStep.jsx';
export { OnboardingInterestsStep } from './OnboardingInterestsStep.jsx';
