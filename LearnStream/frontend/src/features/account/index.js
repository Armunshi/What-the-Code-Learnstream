// Public API surface of the account feature (docs/lanes/acc.json owns
// features/account/**). Created empty in Wave 0 so ACC can fill it in
// without also creating the directory/index.js.
export {};
