// Public API surface of the reviews feature (docs/lanes/rev.json owns
// features/reviews/**). Replaces the W0-A placeholder bodies for the three
// frozen stubs assigned to REV (docs/contracts/stubs.md) — the signatures
// below are unchanged from the stub.
export { CourseRatingHeaderMetric, CourseReviewsSection } from "./CourseReviews.jsx";
export { HomeSocialProof } from "./HomeSocialProof.jsx";
