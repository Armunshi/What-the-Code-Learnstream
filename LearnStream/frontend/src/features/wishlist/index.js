// Public API surface of the wishlist feature (docs/lanes/acc.json owns
// features/wishlist/**). Replaces the W0-A placeholder body of the frozen
// `WishlistButton` stub (docs/contracts/stubs.md): used both standalone
// (course page) and inside CoursePopover — same component, icon only, no
// popover-specific variant, exactly as the frozen signature requires.
export { WishlistButton } from './WishlistButton.jsx';
