// Public API surface of the course (public course page) feature
// (docs/lanes/cat.json owns features/course/**). No frozen stub is assigned
// to this feature directory (CourseCard/CourseGrid live in features/catalog
// instead) — created empty in Wave 0 so CAT can fill it in.
export {};
