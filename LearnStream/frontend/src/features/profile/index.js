// Public API surface of the profile feature (docs/lanes/acc.json owns
// features/profile/**). Replaces the W0-A placeholder body of the frozen
// `PublicProfilePage` stub (docs/contracts/stubs.md), which renders at
// /user/:username. The route itself is added by features/profile/routes.jsx
// (registries.md).
export { PublicProfilePage } from './PublicProfilePage.jsx';
