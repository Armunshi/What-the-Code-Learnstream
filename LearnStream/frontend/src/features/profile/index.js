// Public API surface of the profile feature (docs/lanes/acc.json owns
// features/profile/**). W0-A creates this barrel with a placeholder body
// for the frozen `PublicProfilePage` stub (docs/contracts/stubs.md), which
// renders at /user/:username. ACC replaces only this body in Wave 1; the
// route itself is added by ACC's own features/profile/routes.jsx once it
// exists (registries.md).
export function PublicProfilePage() {
  return null;
}
