// Public API surface of the learn (player) feature (docs/lanes/learn.json
// owns features/learn/**). Created empty in Wave 0 so LEARN can fill it in;
// features/learn/renderers/* is a separate registry LEARN hosts (per-item
// player renderers), unrelated to this barrel.
export {};
